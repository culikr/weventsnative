import React from 'react';
import HomeScreen from './src/screens/home';

import render from 'react-test-render';



it('teste'), () => {
    const rendered = rederer.create(<HomeScreen />).toJSON();
    expect(rendered).toBeTruthy();
}